services:
  - type: web
    name: thronixpro-ai
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    plan: free
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: SECRET_AUTH_TOKEN
        sync: false